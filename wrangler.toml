name = "bread-launcher"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[env.production]
name = "bread-launcher-prod"

# Environment variables (add if needed)
[env.production.vars]

# Build configuration
[build]
command = ""

# Pages configuration
pages_build_output_dir = "dist"

# KV namespace (optional, for caching if needed later)
# [[kv_namespaces]]
# binding = "CACHE"
# id = "your-kv-id"
# preview_id = "your-preview-kv-id"

# D1 database (optional, for analytics if needed later)
# [[d1_databases]]
# binding = "DB"
# database_name = "bread-launcher-db"
# database_id = "your-database-id"
